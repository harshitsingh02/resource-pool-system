{
  "name": "resource-pool-system",
  "version": "1.0.0",
  "scripts": {
    "start": "echo 'Starting via Procfile'",  // This is a placeholder—Procfile overrides it
    "build": "echo 'Build placeholder'"       // Not needed, but helps Railpack recognize
  },
  "engines": {
    "node": ">=16.0.0"  // Critical: Tells Railpack this is a Node.js app & version
  }
}